{
    "name": "@pureform/utils",
    "version": "0.0.0-semantically-released",
    "exports": {
        ".": "./src/index.ts",
        "./*": "./src/*/index.ts"
    },
    "scripts": {
        "test:unit": "with-env 'vitest run --config vitest.config.unit.ts'"
    },
    "dependencies": {
        "@pureform/type-utils": "workspace:*"
    },
    "devDependencies": {
        "@pureform/tsconfig": "workspace:*",
        "@vitest/coverage-v8": "^3.0.5",
        "vitest": "^3.0.5"
    }
}
